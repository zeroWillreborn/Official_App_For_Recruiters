// http://localhost:3001/api/achal/support
const BASE_URI = 'http://localhost:3001/api/achal';

async function getDataFromDb(whatToSend,data) {
    if(whatToSend==="supporter"){
        
    }else if(whatToSend==='reviews'){
        
    }
}